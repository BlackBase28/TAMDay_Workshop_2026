---
- name: AI/MCP remediation - send Slack alert
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  vars_files:
    - ../vars/project_defaults.yml
  vars:
    kernel_cve_radar_remediation_action: send_slack_alert
  roles:
    - kernel_cve_radar_remediation
